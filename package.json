{
  "name": "wallet-profiler",
  "version": "1.0.0",
  "description": "web3 wallet profiler",
  "main": "server.ts",
  "scripts": {
    "start:dev": "npx nodemon",
    "start": "npm run build && node build/src/server.js",
    "build": "rimraf ./build && tsc --project tsconfig.build.json",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Liam Coyle",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.2.2",
    "express": "^4.18.2",
    "rimraf": "^3.0.2",
    "web3": "^1.8.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/node": "^18.11.18",
    "dotenv": "^16.0.3",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
